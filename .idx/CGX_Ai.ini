[DEFAULT]
# All sections below will inherit these values.
# You can override them in each section.
# For example, if you want to use a different model for a specific section,
# you can set the "model" value in that section.
model = gpt-4.1
temperature = 0.7
max_tokens = 500
top_p = 1.0
frequency_penalty = 0.0
presence_penalty = 0.0

[Section1]
# This section will use the default values.
# You can add specific settings here if needed.
# For example:
# model = gpt-4

[Section2]
# This section will also use the default values.
# You can add specific settings here if needed.
# For example:
# temperature = 0.9

[Section3]
# This section demonstrates overriding the default model.
model = gpt-4
# Other settings will still inherit from the DEFAULT section.

[Section4]
# Another example of overriding a setting.
max_tokens = 1000
# Other settings will still inherit from the DEFAULT section.
